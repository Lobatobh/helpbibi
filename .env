DATABASE_URL=file:/home/z/my-project/db/custom.db
SESSION_SECRET=dev_secret_change_me
PAYMENT_WEBHOOK_SECRET=dev_webhook_secret_change_me

# Admin seed login (dev only — never set to true in production).
# When true, POST /api/admin/login accepts admin@helpbibi.local / Admin123!
ADMIN_SEED_ENABLED=true

# Audit log backend: 'memory' (default, in-process ring buffer) or 'database'
# (persist each event to the AuditLog Prisma model, fire-and-forget).
AUDIT_LOG_BACKEND=memory

